# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Build output
.svelte-kit/
build/

# Git worktrees checked out inside the repo: a second copy of the tree, which
# would otherwise be format-checked alongside the real one.
.claude/worktrees/

# Planning material: the prototype and design docs are not ours to format.
scratch/

# Miscellaneous
/static/
