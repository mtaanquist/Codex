# Dependencies
node_modules/

# Build output (SvelteKit)
.svelte-kit/
build/

# Environment and local secrets
.env
.env.*
!.env.example

# Local Claude Code overrides (settings.json is committed; local is not)
.claude/settings.local.json

# Planning prototype: regenerable from Claude Design, kept out of the repo.
# The system-design docs under scratch/ are committed; the prototype is not.
scratch/app-design/

# OS noise
.DS_Store
